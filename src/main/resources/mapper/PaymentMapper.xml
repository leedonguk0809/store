<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.store.repository.mapper.PaymentMapper">

    <insert id="insertPayment" parameterType="com.example.store.domain.Payment">
        INSERT INTO payment (order_id, payment_price, payment_date, bank)
        VALUES (#{orderId}, #{paymentPrice}, NOW(), #{bank})
    </insert>

</mapper>
